# ============================================================
# requirements.txt — Phân Cụm Chủ Đề Văn Bản Tiếng Việt
# Tested on Python 3.10/3.11, Windows 11
# ============================================================

# --- NLP Tiếng Việt ---
underthesea>=9.5.0          # Tách từ (word segmentation) tiếng Việt

# --- Topic Modeling ---
gensim>=4.4.0               # LDA model + CoherenceModel (c_v score)

# --- Machine Learning & Khoa học dữ liệu ---
scikit-learn>=1.8.0         # TF-IDF, KMeans, PCA, t-SNE, LDA (sklearn fallback)
numpy>=2.0.0
pandas>=3.0.0
scipy>=1.17.0               # Thư viện toán học (dependency của gensim, sklearn)

# --- Giao diện & Trực quan hóa ---
streamlit>=1.57.0           # Web app framework
plotly>=6.0.0               # Biểu đồ tương tác (scatter, bar)
matplotlib>=3.10.0          # Word cloud & bar chart tĩnh
wordcloud>=1.9.6            # Vẽ word cloud từ topic keywords
pyLDAvis>=3.4.1             # Bản đồ tương tác phân bố chủ đề LDA

# --- Thu thập dữ liệu (Web Scraping & External Datasets) ---
requests>=2.34.0            # HTTP requests cào bài viết VnExpress
beautifulsoup4>=4.14.0      # Phân tích HTML
kagglehub>=1.0.0            # Tải dataset từ Kaggle (20 Newsgroups)
datasets>=4.0.0             # HuggingFace Datasets (VNTC-10Topics)

# ============================================================
# Tùy chọn nâng cao (Optional)
# Bỏ comment (#) để cài khi muốn sử dụng đầy đủ tính năng.
# Lưu ý: hdbscan, umap-learn cần C++ Build Tools trên Windows.
# ============================================================

sentence-transformers>=3.0.0   # SBERT Embeddings đa ngôn ngữ (paraphrase-multilingual-MiniLM-L12-v2)
torch>=2.0.0                   # PyTorch — backend cho sentence-transformers
bertopic>=0.16.0               # BERTopic topic modeling hiện đại
umap-learn>=0.5.6              # Giảm chiều UMAP (dùng cho BERTopic & Visualization)
hdbscan>=0.8.33                # Phân cụm HDBSCAN (dùng bởi BERTopic)
